<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.oneplatform">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Register"></activity>
        <activity android:name=".ClickedUserPage" />
        <activity android:name=".EditPage" />
        <activity android:name=".SelfPage" />
        <activity android:name=".AddSocialPost" />
        <activity android:name=".AddComment" />
        <activity android:name=".commentActivity" />
        <activity android:name=".SocialPlatform" />
        <activity android:name=".AddPost" />
        <activity android:name=".MainPage" />
        <activity android:name=".forgetPassword" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>